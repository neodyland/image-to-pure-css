[package]
name = "image-to-pure-css"
version.workspace = true
edition.workspace = true

[dependencies]
fxhash = "0.2.1"
image = { version = "0.25.10", default-features = false }
itoa = "1.0.18"

[features]
default = ["std"]
std = ["image/default-formats", "image/rayon"]
